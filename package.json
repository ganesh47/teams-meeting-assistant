{
  "name": "teams-meeting-assistant",
  "version": "1.0.0",
  "private": false,
  "description": "Microsoft Teams meeting assistant with local-only transcription.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "teams-meeting-assistant": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "npm run build",
    "test": "node --test tests/*.test.mjs",
    "sbom": "cyclonedx-npm --output-file sbom.json"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cyclonedx/cyclonedx-npm": "^4.0.3",
    "@types/node": "^24.6.0",
    "playwright": "^1.55.1",
    "typescript": "^5.9.3"
  }
}
