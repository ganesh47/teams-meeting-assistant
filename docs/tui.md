# TUI UX

The project now includes a simple terminal menu interface.

## Run

```bash
teams-meeting-assistant
```

## What it does

- offers a menu for join, offline demo, update check, and install help
- reduces the need to remember raw subcommands
- prints the next concrete command for the selected action

## Current scope

This is a lightweight TUI wrapper, not a full-screen terminal app yet.
